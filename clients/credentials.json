{
  "web": {
    "note": "All credentials have been moved to environment variables for security",
    "warning": "NEVER commit actual credentials to version control",
    "setup_instructions": "Copy values from .env.local at runtime"
  }
}